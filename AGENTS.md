# hermes-agent(此 fork)· Agent 指南

## 先读这三份（按顺序）

1. **`通用协作规则.md`** —— 与这位用户协作的长期规则(跨项目)。
   20 条,每条都对应一次真实事故。**开工前必读,不是参考资料。**
2. **`统一工作记忆.md`** —— Claude Code、Codex 与以后任何 agent 共用的事实索引、
   状态标签、文档冲突表和交接协议。**它负责回答“现在真实做到哪了”。**
3. **`提醒事项/AGENTS.md`** —— 本 fork 唯一活跃项目的架构、铁律与项目状态。

工作分支:`claude/teacher-meeting-reminder-yvslkd`。

事实冲突时遵循 `统一工作记忆.md` 的优先级；不要按 agent 品牌区分记忆，
Claude Code、Codex 和其他 agent 都必须更新同一套文件。

## 最容易犯、代价最大的四条(全文见 `通用协作规则.md`)

- **用户只做两件事:界面点选、整段粘贴不改一字。** 让用户「往程序内部填任何
  值」的方案一律重做 —— 我知道的值预先填进交付物,不知道的值用截图要。
- **API 200 ≠ 用户看得见;代码改对 ≠ 问题解决。** 没部署到生产就是没修好;
  全链路每一层都要确认,别只修看得见的两层。
- **动手前先问「你现在是怎么做的」。** 用户往往早有能跑的方案,重造即作废。
- **绝不删掉用户对生产系统的最后一条访问路径**,收紧安全前先验证还有别的路。

## 仓库现状

仓库其余部分是上游 Hermes agent 的代码;本 fork 的 main 落后上游约两个月,
**不要把本分支的整文件覆盖到生产环境**(会抹掉上游工作)—— 只做摘取式补丁。
