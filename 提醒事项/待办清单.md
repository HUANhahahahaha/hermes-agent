# 待办清单 · 谁在哪儿做什么

> 2026-08-03 更新。**服务器已续费，无断线风险。** 以下均不紧急 ——
> 用户的记提醒流程一直正常运行，从未中断。
> 所有事项都能在微信里让 Clawbot 完成，不需要开 MacBook Air。

---

## 🔴 一、把队列服务源码提交进仓库（唯一真风险）

**为什么要紧**：服务端代码只在 VPS 上，**没有任何备份**。那台机器挂了，
这套唯一的送达链路重建不出来。今天已经体会过「细节只存在于某台机器」
是什么后果。

**在哪做**：微信里让 Clawbot 做（它就在那台 VPS 上）。不需要 MBA。

**怎么说**：
> 把队列服务的源码提交进 hermes-agent 仓库的 `claude/teacher-meeting-reminder-yvslkd`
> 分支，放在 `提醒事项/queue-server/` 下。**不要提交 `~/.reminder-queue/token`
> 或任何密钥**，配置里的密钥用占位符代替，并附一份 README 说明怎么部署和重启。

**做完的好处**：VPS 挂了能重建；以后任何会话都能看懂这套服务。

---

## 🟠 二、部署消费日报修复（用户已确认要做）

**为什么日报还是 0**：修复在分支上，**没有部署到 VPS**。

**在哪做**：微信里让 Clawbot 做。

**怎么说**：
> 拉 `HUANhahahahaha/hermes-agent`（我的 fork，不是 NousResearch）的
> `claude/teacher-meeting-reminder-yvslkd` 分支，应用 `agent/usage_pricing.py`
> 与 `hermes_state.py` 的修复，先跑
> `python3 -m pytest tests/agent/test_usage_pricing.py` 确认通过再重启。

**改了什么**（三处，缺一仍会报 0）：
1. 定价表补齐整个 Claude 5 系列（机器人跑 Sonnet 5，此前表里没有）
2. `COALESCE(?, 0)` → `CASE WHEN ? IS NULL`：未知不再被伪装成 0 元
3. 模型名规范化：显示名（`Claude Sonnet 5`）与未知带日期快照也能认出

⚠️ 这是**估算值**，与账单有小出入。精确对账用 Console 的 Cost 页面下载 CSV。
⛔️ Cost Admin API 那条路对本账号**不可用**（个人账号无法创建 Admin key），
   `cost_report.py` 已标注，除非哪天建了组织，否则不要再去追。

## 🟡 三、评估要不要启用 reminders-brain skill（用户已说「采纳」）

**在哪做**：微信里让 Clawbot 做。

**怎么说**：
> 拉一下 `claude/teacher-meeting-reminder-yvslkd` 分支，读
> `skills/productivity/reminders-brain/` 和 `提醒事项/CLAUDE.md`。
> **先只读评估，别启用**，告诉我加载后你的行为会有哪些变化。

**注意**：Clawbot 现在已经会「解析文字 → 去重 → 写队列」。skill 新增的主要是
**管理层**（默认提前提醒、日程登记、项目停滞追问、每日简报、联系人库），
不是记提醒本身。**不启用也完全不影响日常使用。**

---

## 🟢 四、真正只能在 Mac 上做的（可选，且不急）

只有一件：如果想让每日简报覆盖设备上的全部提醒，
需要确认 **Mac 端上传 `/snapshot` 的那个程序**是否还在正常跑、多久传一次。

其余的 —— 入队端点、鉴权、字段格式、去重协议 —— **都已经拿到了**，
见 `架构.md`，不必再翻 MBA 的会话记录。

---

## ⚪️ 五、随时想起来再说的

- **服务器长期方案**：2026-08-03 已续费（配置 2核2G / 40GB SSD /
  200Mbps 峰值，腾讯云新加坡轻量）。评估结论：**2核2G 是跑 Hermes 的下限，
  不要降配** —— 降到 1G 大概率 OOM，表现为「微信突然没人理」。真正可优化的
  是月流量包（实际负载一个月撑死几百 MB）。若下次续费报价远高于新购价，
  可考虑新购迁移，但需停机，非必要不折腾。

- `项目跟踪.md` 还是空的 → 填了 `scan.py` 的停滞追问才有意义（用户说了「现在
  不想动这个脑子」，别催）。
- `due` 的确切格式没逐字确认 → 文档按 `YYYY-MM-DD HH:mm` 记，以快捷指令
  「从字典值获取日期」能解析为准。
- 本分支有 20+ 提交未合并到主干；合并前先与 `claude/stoic-darwin-agw0p9`
  对齐（那条分支有第二代的 `anchor_node.py`）。

---

## 不用做的事

- ❌ 不用去翻 MBA 的部署会话记录 —— API 规格已补齐。
- ❌ 不用动那台 VPS 的服务配置 —— 它是唯一送达通道，正常跑着就别碰。
- ❌ 不用清理提醒事项 —— CalDAV 那 153 条幽灵数据已清空，你手机上的没动过。
</content>
