# 提醒事项写入架构（现役，勿再造轮子）

> **这是唯一能把提醒送到用户 iPhone 的链路。** 2026-07-28 由用户提供的
> 快捷指令截图确认。此前因为这套架构没被记录，导致一整轮工作走了 CalDAV
> 死路 —— 读这份文件再动手。

## 三代演进（别退回旧路）

| 代 | 方案 | 结论 | 证据 |
|---|---|---|---|
| 一 | iCloud CalDAV | ❌ 死路，写入是假阳性 | 分支 `stoic-darwin` commit `2b3743b3` 已实证；2026-07-28 又被独立复现一次 |
| 二 | iMac 锚节点 + `remindctl` | ⚠️ 能送达，**但要 iMac 24 小时开机** | `stoic-darwin` 上的 `anchor_node.py` / `锚节点-iMac.md` |
| 三 | **队列 + Apple 快捷指令** | ✅ **现役** | 本文件 |

**第三代的存在意义：摆脱「必须有一台机器 24 小时在线」的前提。**
这是用户明确的设计目标，不要为了图省事把方案退回第二代。

⚠️ **第三代的服务端代码没有进 git** —— 部署在 VPS 上，由用户在 MacBook Air
经 Claude Code 完成。API 规格已于 2026-07-28 补全（见下），但**服务端源码
仍未纳入版本管理**，属于单点风险：VPS 挂了没有备份可重建。

## 全链路

```
微信 Clawbot ──入队──▶  队列服务 43.160.249.235:8787
                              │
                              │  GET /claim  （快捷指令定期拉取并认领）
                              ▼
                    Apple 快捷指令（用户 iPhone）
                              │  解析 title / due / notes
                              ▼
                    提醒事项「收集桶」列表 ──▶ 用户看得到 ✅
```

## 快捷指令做了什么（截图逐条还原）

1. `获取 http://43.160.249.235:8787/claim 内容`
2. `重复 URL 的内容中的每一项`：
   - 取 `title` 的值
   - 取 `due` 的值
   - 取 `notes` 的值
   - `从字典值获取日期`（把 `due` 转成日期对象）
   - `将字典值添加到 收集桶（日期）` ← 写入提醒事项
3. `结束重复`
4. `停止并输出 重复结果`；无输出时不执行任何操作

## 队列返回的数据格式

`GET /claim` 返回一个数组，每项至少含三个字段：

```json
[
  {"title": "孙建亚老师 家前采时间确认", "due": "2026-07-02 10:00",
   "notes": "地点：绿洲比华利花园931号楼"}
]
```

- `/claim` 语义是**认领**：取走即出队，重复调用不会拿到同一条。
- ⚠️ **不要为了调试去 GET `/claim`** —— 会把待写入的条目消费掉，
  用户手机就再也收不到那几条了。

## 队列服务 API（2026-07-28 由用户经 Clawbot 确认）

**服务在 VPS 本机监听，Hermes 侧一律走 `http://127.0.0.1:8787`（不出公网）。**
公网 `43.160.249.235:8787` 那一面是给 iPhone 快捷指令 `GET /claim` 用的。

### 端点

| 端点 | 用途 | 安全性 |
|---|---|---|
| `GET /pending` | 查待写队列 | ✅ 只读，不消费 |
| `GET /snapshot` | 查 Mac 端上传的**设备提醒快照** | ✅ 只读，不消费 |
| `POST /push` | 真正入队 | ✍️ 写入 |
| `GET /claim` | 快捷指令认领出队 | ⛔️ **消费型，禁止用于调试/查询** |

### 请求头
```
X-Queue-Token: <token>
Content-Type: application/json
```

### 鉴权
Token 存在**文件** `~/.reminder-queue/token`（**不是环境变量**）。
读取该文件取值，不要把 token 写进代码或本仓库。

### `POST /push` 请求体
```json
{
  "title":   "孙建亚老师 家前采时间确认",
  "due":     "2026-07-02 10:00",
  "remind":  "2026-07-02 09:00",
  "list":    "收集桶",
  "notes":   "人物：孙建亚老师｜地点：绿洲比华利花园931号楼",
  "source":  "hermes-vps",
  "replace": false
}
```

- `due` 到期时间，`remind` 提前提醒时间（两者独立）
- ⚠️ **`list` 字段实际上是被忽略的**（2026-08-04 从 worker 截图确认）。
  手机上的「同步提醒队列」只取 `title` / `due` / `notes` 三个键，从不读 `list`；
  提醒落进哪个列表，完全由「添加新提醒事项」那个动作里**写死的列表名**决定。
  ✅ 2026-08-04 用户展开该动作确认：列表就是**「提醒事项」（默认列表）**，不是 `收集桶`。
  → 删除功能按列表筛选时必须用**实际列表名**，否则筛选恒不命中、静默失效。
- `replace` 为 `true` 时覆盖**队列里**已有的同名条目，用于「改期」场景

  > ⚠️ **2026-08-04 更正：`replace` 碰不到手机上的提醒。**
  > 它覆盖的是**还没被认领的队列记录**，不是设备上那条。因为快捷指令整段
  > 只有一个「添加到收集桶」动作 —— 没有查找、没有移除。条目一旦被认领
  > 落进手机，队列侧就再也改不动、删不掉它。
  >
  > ```
  > 入队 ──► 队列排队 ──► 快捷指令认领 ──► 落进提醒事项 App
  >       ↑            ↑                   ↑
  >  replace 有效   认领的一刹那      过了这条线队列碰不到
  > ```
  >
  > **要能删/改，必须给快捷指令加「查找 + 移除」的一段。**
  > 完整方案见 `增删改-实施方案.md`（仓库侧代码已写完并验证，
  > 待队列服务与快捷指令跟进）。

## 写入前的去重 / 冲突协议（必须照做）

**不要拿到内容就直接 POST。** 既有流程是三步：

1. **`GET /pending`** —— 与队列中待写条目比对，防止重复入队。
2. **`GET /snapshot`** —— 与**设备上已存在的提醒**比对：
   - 标题相似度用 `difflib.SequenceMatcher`，**阈值 ≥ 0.75** 视为同一件事
   - 再比对时间，判定属于「重复 / 冲突 / 改期」
3. **`POST /push`** —— 上面两关都过了才写入；判定为「改期」时带 `replace: true`。

最终由下游消费队列，同步进「提醒事项」。

### ✅ 已由生产实现交叉验证（2026-07-28）

VPS 上的 Clawbot 拿**自己的生产实现** `~/.hermes/bin/queue_push.py` 逐项核对
本文档，四项全部一致：端点（`/pending` `/snapshot` `/push`，均走
`127.0.0.1:8787`）、鉴权（`X-Queue-Token` + `~/.reminder-queue/token`）、
查重（SequenceMatcher ≥0.75 + 时间比对）、禁用 `/claim`。

> 本文档由用户转述的规格写成，中间隔了两道转手，故请生产端复核 —— 这是
> 「拿实现校验文档」而非反过来。以后文档与实现不符时，**一律以实现为准**。

### 下游可以有多个消费者，且不必担心争抢

已知消费者：**iPhone 快捷指令**（`GET /claim` → 写「收集桶」）、
可能还有 **Mac 端 consumer**，另有 **Mac 端程序上传 `/snapshot`**。

**多个消费者同时 claim 是安全的**（用户 2026-07-28 指出，此前本文档的担心是错的）：

- `/claim` 出队语义保证**同一条不会被两个消费者重复拿到** → 不会产生重复提醒。
- 谁认领到哪一条**无所谓**，因为**提醒事项 App 本身通过 iCloud 在设备间合并
  同步** —— iPhone 写的会同步到 Mac，Mac 写的会同步到 iPhone，最终两边一致。

所以这是「分头写入 + 自动合并」，不是「分区丢失」。无需为此加协调机制。

## 已知死路（别再试）

| 通道 | 结论 |
|---|---|
| iCloud CalDAV | ❌ 苹果 2019 年（iOS 13/Catalina）把提醒事项迁进私有仓库，CalDAV 侧只剩一个用户看不到的废仓库。实测：CalDAV 写入的条目手机上不可见；微信写入的条目 CalDAV 里查不到。 |
| 本容器直连队列 | ❌ 43.160.249.235:8787 不在网络白名单内，从云端会话连不上（也不需要连）。 |

## 可行但非现役

| 通道 | 说明 |
|---|---|
| `remindctl` / EventKit | 仅限 macOS 本机，见 `skills/apple/apple-reminders/`。用户当前不走这条。 |

## 铁律

1. **动手前先问「现在是怎么做的」**，不要假设用户没有方案。
2. 验证标准是**用户在 iPhone 上看得见**，不是 API 返回 200。
3. **查询只用 `/pending` 和 `/snapshot`**；`/claim` 是消费型，一读就把用户
   待收的提醒吃掉，任何情况下都不要拿它做调试或查询。
4. **写入前先走完去重协议**（`/pending` → `/snapshot` 相似度比对 → `/push`），
   不要拿到内容就直接 POST，否则会在用户设备上堆重复条目。
5. **不部署、不改配置、不重启该服务** —— 它是唯一送达通道。
6. 现役链路的任何一环变了，**回来改这份文件**。

## 遗留风险

- **服务端源码未纳入版本管理**：只在 VPS 上，无备份。已知生产端入队实现位于
  **`~/.hermes/bin/queue_push.py`**（Clawbot 自述）。建议连同队列服务本体一起
  提交进本仓库（token 等密钥不要提交）。
- **`due` 的确切格式未逐字确认**：文档按 `YYYY-MM-DD HH:mm` 记录，实际以
  快捷指令「从字典值获取日期」能解析为准。
</content>
