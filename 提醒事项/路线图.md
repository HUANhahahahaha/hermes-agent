# 路线图 · 让微信 Clawbot 成为真助理

> ⚠️ **历史路线图：** 保留决策演进和已作废方案，不单独代表当前完成状态。
> 开工与结项以根目录 `统一工作记忆.md`、`提醒事项/AGENTS.md` 和真实实现为准。

> 用户已确认：**下列全部都要做**，一件一件推进。
> 本文件是唯一待办源，别跑偏、别遗漏。每完成一项就更新状态并 commit。
> 状态：⬜ 未开始　🔄 进行中　✅ 已完成　⏸ 阻塞
> 最后更新：2026-07-28

## 底座（已建好，别重复造）
- 分支 `claude/teacher-meeting-reminder-yvslkd` = 唯一真相源
- `README.md` charter 规则 / `日程.md` / `项目跟踪.md`
- `sync.py` iCloud CalDAV 桥（写入逻辑已离线验证）
- `handoff.sh` + `.claude/hooks/` 两机自动交接
- 微信入口：用户微信里的 Hermes Clawbot（`gateway/platforms/weixin.py`）

---

## 🛑 读这一段再动手（2026-07-28 的惨痛教训）

**用户手机上早就有一套能跑的写入方案** —— Apple 快捷指令（Shortcuts），
由更早的会话（Fable 5）生成并由用户导入，定期执行写入。微信 Clawbot
发过去的提醒能落进手机，走的就是这条既有链路。

**本次会话（Opus 5）没问过一句「你现在是怎么写进去的」**，直接假设没有可用
方案，去搭了一座 CalDAV 桥 —— 而那条路从 2019 年起就是死的。结果：
一整轮工作作废，用户白花时间。

**给后来者的硬性要求：**
1. **动手前先问「现在是怎么做的」**，不要假设用户没有方案。
2. 验证标准是**用户在 iPhone 上看得见**，不是 API 返回 200。
3. 已知可行通道：**Apple 快捷指令**（现役）、`remindctl`/EventKit（macOS）。
   **已知死路：iCloud CalDAV**（苹果私有仓库，第三方读写不到）。
4. 那套快捷指令**不在本仓库里**，集成点需要向用户确认。

## 第 0 件：iCloud 写入 — ❌ 方向错误，已作废（2026-07-28）
- 原记「✅ 已完成」是错的：写入的是 CalDAV 废仓库，用户手机上看不到。
- 对照实测：微信写入的「迪桑特」手机可见但 CalDAV 查无；CalDAV 写入的
  「孙建亚老师」CalDAV 有但手机看不到。两仓库完全隔离。
- 已清理该废仓库遗留的 153 条僵尸数据；存档见 scratchpad。
- **正确方向：接入用户既有的快捷指令链路**，不要另起炉灶。

<details><summary>原「已完成」记录（保留以存档）</summary>
- iCloud CalDAV 全链路打通，首条真实提醒已写入手机「HERMES收件」列表。
- 修掉一个真 bug：iCloud 要求绝对闹钟触发器必须是 UTC **且**带
  `VALUE=DATE-TIME` 参数，icalendar 默认生成的裸 `TRIGGER:...Z` 会被 403。
- ⚠️ 遗留：该提醒日期 2026-07-02 **已过期**（当时创建时是未来，今天已 7-28）。
  需用户确认是改期还是标记完成 —— 未擅自改动。

</details>

---

## 🔥 第一梯队（收益最大，优先做）

### 📌 先分清两个不同的运行环境（别再混淆）
| | 模型 | 说明 |
|---|---|---|
| 网页会话（claude.ai/code） | **Opus 5** | 我们对话的地方，**不产生那份 API 账单** |
| **微信 Hermes 机器人** | **Sonnet 5** | 账单 + 消费日报 + 下面所有成本分析的对象 |

账单（api_key = 提醒事项Hermes微信）全程只出现 Haiku 4.5 → Fable 5 →
Sonnet 5，**从未出现 Opus**。最后一天 2026-07-27 为 Sonnet 5。

### 🚫 「智能分流」是个伪命题 —— 该建议作废（2026-07-28 查证）
早先建议「杂活分流到便宜模型」，查实现后发现站不住：

- **Hermes 没有按问题难度选模型的路由器**，不存在这种东西。用户在微信发的
  每一句话都走同一个主模型。
- 它有的是 `agent/auxiliary_client.py` 的**辅助任务**机制，只覆盖四类**副业**：
  `vision` / `web_extract` / `session_search` / `compression`。
  这四类按**是否调用了对应工具**结构性判定，与问题难度无关。
- **且这四类默认就是 Gemini Flash**。配置原文：「By default these use Gemini
  Flash … You do NOT need to change anything here for normal usage.」
  → 早先推荐的「分流」**默认早已开启**，等于推荐一个已开着的功能。
- 「预判难度」本身也不可靠：误判不对称 —— 难题错发便宜模型会**自信地给错
  答案且无人察觉**，不像超时/报错会暴露。稳妥做法是一个够好的默认，
  而非预分类。

**→ 真实决策只剩一个：主模型用哪个。** 结论见下方成本分析。

### 🚫 供应商决策（2026-07-28 查证，两条早先建议已作废）

**① Anthropic 订阅 OAuth：绝对不要开 —— 已被官方封禁**
- 2026-01-09 起服务端拦截第三方客户端的订阅 OAuth；2026-02-19/20 法律条款
  明确写入禁止（Free/Pro/Max 的 OAuth 仅限 Claude Code 与 claude.ai）；
  2026-04-04 正式实施。OpenClaw / OpenCode / Cline / 官方 Agent SDK 均受影响。
- **Hermes 的实现正踩在禁区上**：它读取 `~/.claude/.credentials.json` 与
  macOS 钥匙串的 Claude Code 凭据（`agent/anthropic_adapter.py:857`），
  且该流程**没有任何警告**（Gemini 那条反而有）→ 仓库代码停留在政策更新前。
- **结论：维持 API key。** 早先「主力换订阅模式」的建议作废。
- 想用订阅价值，唯一合规路径是直接跑官方 Claude Code CLI 本身。

**② Gemini：用 API key，不要用订阅 OAuth**
- 仓库 `google-gemini-cli` OAuth 登录前会弹警告：Google 视其为违反政策，
  已有账号被限制（`hermes_cli/main.py:3842`）。
- 详见 `Gemini接入.md`（含配置步骤、模型选择、实测成本对比）。
- ✅ key 已实测可用：Flash 全系 + `gemini-3.1-pro-preview` 均能调通。
- **实测成本（已计入思考 token，早先 $1.90 的估算作废）**：

  | 模型 | 月成本 | 对比 |
  |---|---|---|
  | Sonnet 5（现状） | $7.33 | 基准 |
  | Gemini 3.1 Pro | $8.89 | **更贵** |
  | Gemini 3.6 Flash | $4.00 | 省 45% |

  思考 token 占总量 88~91% 且按输出计价，是早先低估的原因。
  Pro 另有延迟硬伤：一句琐碎问题耗时 54 秒。

### ⚠️ 缓存 TTL：结论已推翻，**维持 5m，不要改 1h**
早先建议改 `cache_ttl: "1h"`，用真实账单复算后**该建议是错的**：
- Anthropic 只有 5m / 1h 两档，**1h 即上限**，跨不过用户几小时~几天的间隔。
- 实测：写入折基准价 $6.00、读出折基准价 $11.50 → **平均每份缓存被读 1.92 次**，
  缓存是**净赚**的（不缓存 $17.89 → 现状 $9.04，省 $8.85）。
- 改 1h 后写入premium 从 1.25x 涨到 2x，光写入 $12.00 就超过现全部账单 →
  **反而贵 $4.50**。
- 早先「83% 是浪费」的说法也不成立：83% 只是缓存写的占比，不等于浪费。
- 真正省钱杠杆（订阅与分流两条均已作废，见上）：**只剩「精简每次重传的
  prompt」**，以及换主模型（但 Gemini Pro 更贵、Flash 有能力风险）。
  结论：**维持 Sonnet 5 是当前最优解**，省那 $3 不值得拿核心任务的
  准确率去赌。

### 💰 主模型选型实测（2026-07-28）
按真实用量重算，Opus 5 与 Opus 4.8 **定价完全相同**（$5/$25）：

| 模型 | 月成本 | 差额 |
|---|---|---|
| Haiku 4.5 | $2.44 | −$4.89 |
| **Sonnet 5（现状）** | **$7.33** | — |
| Opus 4.8 / Opus 5 | $12.21 | +$4.89（+67%） |
| Fable 5 | $24.43 | +$17.10 |

- 选 Opus 5 还是 4.8 是**能力问题，不是钱的问题**（同价）。
- ⚠️ 但 **Opus 5 默认开启思考**（4.8 省略该参数则不思考），输出会变多：
  思考若为输出的 5 倍 → $15.96；10 倍 → $19.71；20 倍 → $27.21。
  要上 Opus 5 就得显式配置 thinking，否则账单会悄悄涨。
- **建议维持 Sonnet 5**：机器人的核心任务是「读懂一句中文提醒并正确安排」，
  Sonnet 5 已足够；+67% 买的能力这个任务用不上。

### 1. ✅ 修复消费日报（已完成 2026-07-28，commit 93b4e4d）
**已定位根因（2026-06-22 排查确认）：**
- **路径 A｜定价表缺 fable**：`agent/usage_pricing.py` 无 `claude-fable-5` 条目 →
  `estimate_usage_cost` 返回 `None`(unknown)。实测：
  `claude-fable-5 → cost=None`；`claude-opus-4-8 → 10.00`；`claude-sonnet-4-6 → 6.00`
- **路径 B｜None 被伪装成 0**：`hermes_state.py:1172` 与 `:1193`
  `estimated_cost_usd = COALESCE(?, 0)` → `COALESCE(NULL,0)=0`，
  「定价未知」被当成「花了 0 元」入库，日总结求和即为 0。
- **路径 C｜订阅模式**：`usage_pricing.py:782` `subscription_included` 直接返回 0，
  标签 `included` —— 这是**正确行为**，不是 bug（Pro/Max 订阅不走 API 计费）。

**已做（commit 93b4e4d）：**
- [x] 补齐**整个 Claude 5 系列**（不只 fable）：`claude-opus-5` $5/$25、
      `claude-sonnet-5` $3/$15、`claude-fable-5` + `claude-mythos-5` $10/$50，
      含 fast 变体；缓存倍率 5m写1.25x / 1h写2x / 读0.1x
- [x] 区分「未知」与「0」：`COALESCE(?, 0)` → `CASE WHEN ? IS NULL`，
      未知保留原值并由 `cost_status='unknown'` 标记，不再伪装成 0
- [x] 测试：test_usage_pricing.py (11) + hermes_state 系列 (282) 全通过

**待做：**
- [ ] 日报推送区分展示：真实花费 / 订阅内(included) / 未知(n/a)
- [ ] 加按模型拆分 + 超预算预警
- [ ] 用户自查命令：Mac 上 `hermes cost --verbose`；权威账单看 console.anthropic.com

### 2. ✅ 微信转发即建提醒（已封装，待部署到微信那台）
- 转发任意文字 → 解析 → 写进 iCloud 提醒事项 → 微信回执
- 顺带把**采访行程/地址/联系人**结构化沉淀，支持「上个月见了谁」这类查询
- 依赖：第 0 件跑通 + 封装成 Hermes skill 部署到微信那台

### 3. ✅ 项目停滞主动追问（scan.py 已就绪，待填项目数据）
- charter §4 已定义规则；挂 cron 每周一扫 `项目跟踪.md`
- 停滞超 7 天 → 推微信「X 项目 N 天没动静了，卡在哪？」
- 参考：`hermes cron create "0 9 * * 1" "..." --deliver weixin`

---

## ⚡ 第二梯队

### 4. 🔄 采访素材管家（接线方案已出：`素材与文档接线.md`）
微信发语音/照片/链接 → 自动转文字、归档到对应采访项目、生成摘要。
（用户做家前采，素材天然分散，这块价值高）

### 5. ✅ 每日简报（brief.py 已就绪并实跑验证）
早上推送：今日日程 + 待确认事项 + 昨日进展 + 今天该催谁。

### 6. ✅ 随手记自动归类（规则已写进 reminders-brain skill）
微信里随口一句 → 判断是待办/素材/项目笔记 → 自动归到对应文件。

---

## 🧠 第三梯队

### 7. ✅ 联系人 / 嘉宾库（`联系人.md` 已建）
采访对象的地址、时间、状态自动沉淀成可查表。

### 8. 🔄 微信里直接指挥写代码（能力已存在，接线说明见 `素材与文档接线.md`）
手机一句「修一下 X」→ 开会话干活 → 推分支 → 回结果。

### 9. 🔄 文档 / 合同处理（接线方案已出：`素材与文档接线.md`）
转发 PDF → 自动摘要 + 提取关键日期 → 关键日期自动变成提醒。

---

## 推进纪律
1. **一次只推一件**，做完更新本文件状态再开下一件。
2. 每件事完成后：更新 `进度.md` 状态板 + commit + push。
3. 不确定优先级时按梯队顺序；用户临时插队的需求也登记到这里，别只存在于对话里。
</content>
