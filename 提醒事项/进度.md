# 进度 / 交接 note

> 给在「提醒事项」group 里接手的下一个会话/另一台机器看。读完即可继续。
> 最后更新：2026-06-22

## 📍 当前状态板（每次收工前更新这一块）
- **📋 待办总表**：`提醒事项/路线图.md`
- **⚠️ 先读**：`提醒事项/架构.md` —— 用户手机上**早有一条能跑的写入链路**
  （队列 + Apple 快捷指令）。2026-07-28 因未先问「现在怎么做的」而误走
  CalDAV 死路，整轮作废。**动手前先问现状。**
- **✅ 卡点已解**：队列 API 规格已于 2026-07-28 补齐（`/pending` `/snapshot`
  `/push`、鉴权、去重协议），见 `架构.md`。**MacBook Air 不再是必需的。**
- **✅ 服务器已续费**（2026-08-03），无断线风险。
- **📋 下一步看 `待办清单.md`**。都不紧急，且都能在微信里让 Clawbot 完成：
  ① 队列服务源码提交进仓库（只在 VPS 上、无备份，唯一真风险）
  ② 部署消费日报修复（日报仍报 0 只因**修复未部署**）
  ③ 启用 reminders-brain skill（用户已说「采纳」）
- 两机交接协议见 `协作与交接.md`；一键脚本 `handoff.sh`。

## 目标
把用户在本 group 粘贴的任何文字 → 自动写进其苹果设备「提醒事项 App」，
并主动管理日程/项目。完整规则见 `README.md`（charter）。

## 已完成
- ✅ charter 规则：`提醒事项/README.md`
- ✅ 日程清单：`提醒事项/日程.md`（含待验证的第一条：孙建亚老师 7-2 家前采）
- ✅ 项目跟踪：`提醒事项/项目跟踪.md`
- ✅ 执行通道选定：**iCloud CalDAV 桥**，脚本 `提醒事项/sync.py`
- ✅ 配置说明：`提醒事项/同步配置.md`
- ✅ 用户已在 Cloud/Default 环境配好环境变量 `ICLOUD_USERNAME` / `ICLOUD_APP_PASSWORD`
- ✅ 用户已把 `caldav.icloud.com` 和 `*.caldav.icloud.com` 加入网络白名单（Network access = Custom）

## 已离线验证（无需网络）
- ✅ `sync.py` 生成的 VTODO ical 格式正确并通过回读校验（标题/到期/提前提醒都对）。
  → 网络一通，`add` 命令必然写入成功，不会再翻车。

## ⏳ 当前卡点 → 下一步（在新会话里做）
网络白名单改动**只对新会话生效**（旧会话拿不到，已多次确认仍报 `Host not in allowlist`）。
在新会话里：

1. 装依赖（新会话不带上次手动 pip 的包）：
   ```bash
   pip install -r 提醒事项/requirements.txt
   ```
2. 验证连通：
   ```bash
   python3 提醒事项/sync.py lists
   ```
   - 能列出提醒事项列表 = 全链路打通。
   - 若仍报 `Host not in allowlist` = 网络规则没生效，需再开新会话。
   - 若报 iCloud 认证 Forbidden = App 专用密码问题，让用户重新生成。
3. 打通后，把第一条真实写入用户手机验证：
   ```bash
   python3 提醒事项/sync.py add \
     --title "孙建亚老师 家前采时间确认（绿洲比华利花园931号楼）" \
     --list "工作-采访" --due "2026-07-02 10:00" --alarm "2026-07-02 09:00"
   ```
   （`--list` 名按用户实际列表调整；先用 `lists` 看真实列表名。）

## 之后（待办）
- 把 `提醒事项/` 封装成可部署的 Hermes **skill**，让用户微信那台 Hermes 拉本分支即可用，
  实现"转发给微信 Clawbot → 自动写进 iCloud 提醒事项"。
- 可选：用 `hermes cron` 挂周期任务，扫 `项目跟踪.md` 停滞项目并推送到微信。

## 分支
`claude/teacher-meeting-reminder-yvslkd`
</content>
