# 进度 / 交接 note

> 给在「提醒事项」group 里接手的下一个会话看。读完即可继续。
> 最后更新：2026-06-22（全链路已打通并验证）

## 目标
把用户在本 group 粘贴的任何文字 → 自动写进其苹果设备「提醒事项 App」，
并主动管理日程/项目。完整规则见 `README.md`（charter）。

## 已完成
- ✅ charter 规则：`提醒事项/README.md`
- ✅ 日程清单：`提醒事项/日程.md`（第一条：孙建亚老师 7-2 家前采，已写入手机）
- ✅ 项目跟踪：`提醒事项/项目跟踪.md`
- ✅ 执行通道选定：**iCloud CalDAV 桥**，脚本 `提醒事项/sync.py`
- ✅ 配置说明：`提醒事项/同步配置.md`
- ✅ 用户已在 Cloud/Default 环境配好环境变量 `ICLOUD_USERNAME` / `ICLOUD_APP_PASSWORD`
- ✅ 用户已把 `caldav.icloud.com` 和 `*.caldav.icloud.com` 加入网络白名单（Network access = Custom）
- ✅ **全链路验证通过**（2026-06-22 本会话）：
  - `pip install caldav icalendar` → `sync.py lists` 成功列出列表
  - 第一条真实提醒已写入并同步：**孙建亚老师 家前采时间确认（绿洲比华利花园931号楼）**
    @ 2026-07-02 10:00，提醒 09:00 → 写进 iCloud「提醒事项」列表，`show` 已能读回，roundtrip 校验通过
- ✅ 修了两个 iCloud 兼容性坑（见下「踩坑记录」）

## 真实列表名（用户账号）
`lists` 实测可写 VTODO 列表：**提醒**、**huan.montoya@gmail.com**、**ifoon@me.com**、
**提醒事项**（默认写这个）、**Reminders ⚠️**。（`HUAN` 是 VEVENT 日历，不能写提醒。）

## 踩坑记录（已在 sync.py 修好）
1. **VALARM 绝对时间 trigger → Forbidden**：iCloud 只接受相对 `DURATION` 的 trigger。
   `cmd_add` 现已把 `--alarm` 的绝对时间换算成相对 `--due` 的偏移。
2. **`cal.todos()` → 500 ReportError**：caldav 3.x 发的带时间范围 REPORT 被 iCloud 拒。
   `cmd_show` 改用 `cal.search(todo=True)`，客户端过滤已完成 / 空 summary，按 due 排序。

## 已封装成 Hermes skill（2026-06-22 本会话）
- ✅ skill：`skills/apple/icloud-reminders-caldav/`
  - `SKILL.md`（frontmatter + 用法 + iCloud 踩坑说明 + 安全）
  - `scripts/icloud_reminders.py`（与 `提醒事项/sync.py` 同源）
  - 与 Mac 专用的旧 skill `apple-reminders`（需 remindctl）互补：本 skill 不需要 Mac。
- ✅ smoketest 通过：默认列表写入 + show 读回 + 清理。
- ✅ **默认列表 = `ifoon@me.com`**（用户指定）；可用环境变量 `REMINDER_LIST` 覆盖，
  `--list` 优先级最高。

## 之后（待办）
- 微信那台 Hermes 拉本分支后，`hermes setup` 配好 `ICLOUD_USERNAME`/`ICLOUD_APP_PASSWORD`
  即可用，实现"转发给微信 Clawbot → 自动写进 iCloud 提醒事项"。
- 可选：用 `hermes cron` 挂周期任务，扫 `项目跟踪.md` 停滞项目并推送到微信。

## 分支
本会话工作在 `claude/stoic-darwin-agw0p9`（上游交接 note 原写 `claude/teacher-meeting-reminder-yvslkd`）。
</content>
