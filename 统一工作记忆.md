# 跨智能体统一工作记忆

> 供 Claude Code、Codex 及以后任何 agent 共用。这里记录持久事实与交接状态，
> 不保存聊天流水账，不保存 token、密钥、真实队列内容或用户隐私数据。
> 最后核对：2026-08-05，Codex 对当前分支全部 56 个增量提交及核心实现交叉审阅。

## 一、每次接手的读取顺序

1. 根目录 `AGENTS.md`
2. `通用协作规则.md`（用户长期偏好与事故规则）
3. 本文件（跨智能体事实索引）
4. 活跃项目的 `AGENTS.md`；目前是 `提醒事项/AGENTS.md`
5. 与本次任务直接相关的真实代码、生产只读证据和专项文档

Claude Code、Codex 只是不同执行者，不建立各自独立的“私有真相”。可复用的新事实
必须写回本文件或项目 `AGENTS.md`，下一位 agent 不应依赖上一段聊天才能接手。

## 二、事实优先级与状态标签

事实冲突时，从高到低采用：用户刚确认的现场事实；生产实现或安全的只读证据；
当前分支的真实代码与测试；最新统一记忆与项目 `AGENTS.md`；专项方案和历史状态板；
旧聊天总结、截图推测与已作废方案。

- **[已验证-生产]**：在真实生产链路验证过
- **[已验证-设备]**：用户在实际设备界面确认过
- **[已验证-隔离]**：只在临时目录或测试环境验证
- **[仓库完成]**：代码已提交，但不等于生产上线
- **[未验证]**：有记录或推断，但没有当前现场证据
- **[已作废]**：明确不能再走的方案

任何 agent 都不得把低等级状态写成高等级状态。API 200 不等于设备送达；
代码已提交不等于生产已修复。

## 三、用户协作方式（长期有效）

完整规则见 `通用协作规则.md`。最关键的执行摘要：

- 用户只做熟悉界面里的点选，或整段粘贴且一字不改。
- 不交付开放式填空；已知值预填，未知值优先用截图取得。
- 先问或先查“现在怎么做”，不要假设用户没有现役方案。
- 方案对比是 agent 的作业；默认给一个推荐和一句理由。
- 不删除最后一条生产访问路径，不擅自扩大授权。
- 文档与实现不符时以实现为准，再把持久结论写回统一记忆。

## 四、提醒事项项目的现役真相

用户在微信向 VPS 上的 Hermes Clawbot 发送日程内容，由 Clawbot 解析并经本机
队列服务入队；iPhone 快捷指令“同步提醒队列v3”定时消费，最终写入 Apple
提醒事项 App 的“收集桶”。删除先执行，新增后执行。

- **[已作废]** iCloud CalDAV：写入假阳性，用户设备不可见。
- **[非现役]** iMac + `remindctl`：可行但要求机器常开，不能替代第三代。
- **[已验证-生产]** Hermes 侧使用 `127.0.0.1:8787`；公网面仅供手机消费者。
- 查询只用 `/pending`、`/snapshot`；任何 `/claim*` 都是消费型，禁止调试调用。
- 新增先查 pending，再查 snapshot；标题相似度阈值 0.75。
- 删除/改期阈值 0.85；先查 pending 再查 snapshot；0 条或多条命中不执行。
- 删除前必须把设备快照中的原始标题念给用户确认。
- 改期等于先删后加，手机 worker 必须保证删除段先执行。

### 当前状态

- **[已验证-生产]** `op=delete` 独立队列、`/cancel`、非法 op 返回 400。
- **[已验证-设备]** “同步提醒队列v3”已交付；记录称删除段与用户原 9 个新增动作
  组合，原有可运行部分逐字节保留。
- **[已验证-生产]** Clawbot 的删除/改期逻辑已布置；`queue_client.py` 是仓库参考实现。
- **[仓库完成]** VPS 队列真实源码已脱敏归档到 `提醒事项/queue-server/`。
  来源提交 `cbfbd4b1cd95142a78fccacd219a1dcfaadf262c`，入库提交
  `e56f7ec41726ceecdee5ac67d136e3dc2978d389`。
- **[仓库完成][已验证-隔离]** `GET /claim-delete` 路径别名已加入归档源码；
  两项隔离回归通过，提交 `2e37f294168c75cbd96f95ccebb76a0096a78f7c`。
- **[未验证]** `/claim-delete` 是否已部署到生产。未部署前不能说路径别名可用。
- **[已完成]** 阿里云百炼官方页已出现稳定模型 ID `qwen3.8-max`；用户已收到通知，
  因触发条件已经发生，不再创建长期发布监控 cron。

## 五、已经识别的文档与实现分叉

1. `提醒事项/README.md` 第 5 节仍写 CalDAV；这是历史遗留，执行时作废。
2. `提醒事项/进度.md` 后半仍包含 2026-06 CalDAV 卡点；不是当前状态。
3. `提醒事项/路线图.md` 混合历史建议、已推翻结论与当前方向；只用于理解演进。
4. 手机快捷指令存在多代生成方式：
   - `build_worker_v2.py` 使用 `POST /claim?op=delete`
   - `splice_delete_block.py` 当前也由原 claim URL 派生查询参数形式
   - `build_delete_shortcut.py` 使用 `POST /claim-delete`
   - 当前归档服务源码只实现 **GET** `/claim-delete`
5. 仓库旧生成器不能自动代表用户手机当前 v3。任何手机端或别名工作，必须先拿
   当前导出的真实 v3 或可信现场证据核对 URL、HTTP 方法、列表名和 plist 字段；
   只做摘取式拼接，不从头重写。
6. 本 fork 的基线落后生产和上游。向 VPS 交付只能摘取目标补丁，禁止整文件覆盖。

## 六、其他已做工作与未验证状态

- 消费日报现场输出已证明旧修复未生效：2026-08-06 今日有 347293 输入、4331 输出、
  519415 缓存读，却仍报告 ¥0。根因之一已落实到代码：数据库里的字面值
  `billing_provider=unknown` 不会触发 provider 推断，且旧 `COALESCE` 写法无法用后续
  已知 provider 覆盖它。
- **[仓库完成][已验证-隔离]** 本地预估方案已补齐：读取时把 `unknown` 当缺失并按
  Claude 模型推断 Anthropic；写入时让 provider/mode 占位值自愈；Sonnet 5 限时价
  到 2026-08-31 后自动切标准价；新增 `提醒事项/estimated_cost_report.py`，任何
  非零 token 的未知价格都报错，绝不显示假 ¥0。真实日报样例回归及 5 项隔离测试通过。
- **[未验证]** 上述修复是否已摘取部署到 VPS、花费日报 job `39fff172082f` 是否已改为
  直接执行新脚本、生产 gateway 是否已人工重启。未完成这三项前不得称生产已修复。
- `reminders-brain`、`brief.py`、`scan.py` 已存在；**[未验证]** Clawbot 是否正式启用。
  项目跟踪表目前基本为空，停滞扫描没有足够业务数据。
- **[未验证]** Mac 端 `/snapshot` 上传程序当前是否运行、刷新频率及数据新鲜度。
- VPS 日常操作优先交给微信 Clawbot；它不能重启自己的 gateway。gateway 重启必须
  在腾讯云网页终端或已验证 SSH 会话里人工执行。
- 必须保留腾讯云网页终端这一带外访问路径。

## 七、跨智能体更新协议

1. 用状态标签写清证据等级，不写“应该已经”“大概完成”。
2. 架构和铁律更新项目 `AGENTS.md`；跨项目规则更新 `通用协作规则.md`；
   状态与冲突更新本文件。
3. 同一事实只指定一个主记录位置，其他文件用链接，避免复制后漂移。
4. 旧文档若会诱导危险操作，先加醒目的历史或作废标记，再保留决策背景。
5. 收工前把文档与代码放在同一提交中推送；本地未 push 不算完成交接。
6. 不把密钥、token、真实提醒数据、日志或用户隐私写入记忆文件。

## 八、下次继续工作的最短入口

- 继续队列别名：先只读核对生产 handler 与当前手机 v3；不得用 `/claim*` 测试。
- 继续消费日报：把本次已验证补丁摘取部署到 VPS，令 job `39fff172082f` 直接运行
  `提醒事项/estimated_cost_report.py`，人工重启 gateway 后以非零真实日报验收；
  不再尝试个人账号不可用的 Anthropic Admin Cost API。
- 继续主动管理：先确认 reminders-brain 与 snapshot 上传状态，再启用 cron 或简报。
- 新的提醒事项操作：遵循 `提醒事项/AGENTS.md` 的去重、确认和送达回执规则。
